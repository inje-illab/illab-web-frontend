import React from 'react';
import NetworkImage from "../../assets/network.png";

const Network = ({className}) => {
    return (
        <img src={NetworkImage} className={className}/>
    );
};

export default Network;